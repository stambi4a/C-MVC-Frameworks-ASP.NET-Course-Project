@using BindingModels
@model CountryBindingModel

@{
    ViewBag.Title = "Create new country";
}

@using (Html.BeginForm("Create", "Countries", new { Area = "LocationAdmin" }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <div class="row">
        <div class="col-lg-4 col-md-5 col-sm-6">
            <div class="form-horizontal">
                <h4>@ViewBag.Title</h4>
                <div class="responsive">
                    <table class="table-striped">
                        <tr>
                            <td class="left-cell col-lg-2 col-md-3 col-sm-4">
                                @Html.LabelFor(model => model.Name, new { @style = "font-weight: normal;" })
                            </td>
                            <td class="right-cell col-lg-2 col-md-3 col-sm-4">
                                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control", placeholder = "Enter name..." } })
                                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                            </td>
                        </tr>
                    </table>
                </div>
                <br />
                <div class="form-group">
                    <div class="col-lg-3 col-md-3 col-sm-3">
                        <input type="submit" value="Create" class="btn btn-default" />
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-4 col-md-4 col-sm-4 col-xs-6">
                        @Html.ActionLink("All countries", "Index", null, new { @class = "btn btn-primary" })
                    </div>
                </div>
            </div>
            <br />
            <br />
        </div>
    </div>
}

<div class="col-md-4">
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
